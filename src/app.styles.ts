import { styled } from "@/stitches.config";

export const Container = styled('main', {
  width: '100%',
  maxWidth: '46rem',
  margin: '0 auto',
})

export const Form = styled('form', {
  position: 'relative',
  top: '-2rem',
  display: 'flex',
  alignItems: 'center',
  gap: '0.5rem'
})