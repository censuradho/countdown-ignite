import { styled } from '@/stitches.config'

export const Container = styled('header', {
  background: '$gray700',
  height: '12.5rem',
  width: '100%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center'
})